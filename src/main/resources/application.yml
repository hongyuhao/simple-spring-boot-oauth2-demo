server:
  port: 8080

---
logging:
  level:
    org:
      springframework:
        security: DEBUG

---
spring:
    datasource:
          type: com.alibaba.druid.pool.DruidDataSource
          url: jdbc:mysql://127.0.0.1:3306/test?characterEncoding=utf8&useUnicode=true
          username: root
          password:
          driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  config-location: classpath:SqlMapConfig.xml
  mapper-locations: classpath:sqlmap/*.xml